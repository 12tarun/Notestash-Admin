@*@model Notestash_Admin.Models.notesData*@

@{
    ViewBag.Title = "SelectedUser";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@**@

@*@if (ViewBag.CannotDelete == true)
{
   
}*@

<h2>SelectedUser</h2>

@Html.ValidationMessage("CannotDelete", new { @class = "text-danger" })

<i class="fas fa-trash-alt" style="color:red;float:right;cursor:pointer" title="Delete this account" data-toggle="modal" data-target="#myModal2"> </i>
            <!-- The Modal -->
<div class="modal fade" id="myModal2">
    <div class="modal-dialog">
        <div class="modal-content">

            <!-- Modal Header -->
            <div class="modal-header">
                <h4 class="modal-title" style="color:red">Warning!</h4>
                <button type="button" class="close" data-dismiss="modal">&times;</button>
            </div>

            <!-- Modal body -->
            <div class="modal-body">
                @*@Html.Action("SignUp", "Register")*@
                <h4>Are you sure you want to delete this account permanently?</h4>
            </div>

            <!-- Modal footer -->
            <div class="modal-footer">
                <button type="button" class="btn btn-danger" onclick="location.href='@Url.Action("DeleteSelectedUser", "UserData", new { id = Model.Id })'">Yes</button>
                <button type="button" class="btn btn-secondary" data-dismiss="modal">No</button>
            </div>

        </div>
    </div>
</div>